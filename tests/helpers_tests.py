from nose.tools import *
from hmm import de_universal_corpus


#def test_de_universal_corpus():
    #train, dev, test = de_universal_corpus()
    #assert_is_instance(train, list)
    #assert_is_instance(dev, list)
    #assert_is_instance(test, list)
